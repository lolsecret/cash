from django.urls import path

from . import views

urlpatterns = [
    # path('banks/', views.BankView.as_view()),
    # path('issuance_plan/', views.IssuancePlanView.as_view()),
    # path('document/', views.DocumentListView.as_view()),
    # path('faq/', views.FAQListView.as_view()),
]
